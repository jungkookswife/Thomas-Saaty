import numpy as np

def validate_input(value):
    try:
        value = float(value)
        if value <= 0:
            raise ValueError
        return value
    except ValueError:
        print("Ошибка ввода! Введите положительное число.")
        return None
